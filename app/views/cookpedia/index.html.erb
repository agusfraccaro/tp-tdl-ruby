<div style="display: flex">
    <h1> Recetas </h1>
    <h1>
        <%= button_to new_recipe_path, method: :get, class: "btn" do%>
            <i class="bi bi-plus-square"></i>
        <% end %>
    </h1>
</div>

<%= render 'layouts/table', recipes: @recipes, buttons:false %>