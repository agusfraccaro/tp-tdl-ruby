<div class="row m-0 justify-content-center align-items-center">
    <div class="col-md-6 col-auto p-5">  
        <h1>Registrate</h1>
        <%= link_to "¿Ya tenes una cuenta? Ingresá.", login_path %>
        <%= form_with model: @user, url: cookpedia_nueva_receta_path do |form| %> 
            <%= render "layouts/msj_errores", form: form %>
            
            <div class="mb-3">
                <%= form.label :nombre %>
                <%= form.text_field :nombre, class:"form-control" %>
            </div>
            
            <div class="mb-3">
                <%= form.label :desripcion %>
                <%= form.text_area :desripcion, class:"form-control" %>
            </div>

            
            <%= form.submit "Crear receta", class: "btn btn-primary" %>
        <% end %>
    </div>
</div>