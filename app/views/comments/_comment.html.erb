<li class="list-group-item">
    <h5> Usuario: <%= User.find_by(id: comment.user_id).nombre %> </h5>
    <p>
        <%= comment.content %>
        <%= button_to recipe_comment_path(comment), method: :delete, class: 'btn btn-danger' do %>
            <i class="bi bi-trash"></i>
        <% end %>
    </p>
</li>